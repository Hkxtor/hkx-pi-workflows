node_modules/
.DS_Store
*.log

# Local pi / agent state (may hold MCP tokens after apply-mcp-profile)
.pi/
.pi-subagents/

# Local-only smoke / scratch tests not part of the package
scripts/_smoke/

# Secrets and local env (keep examples trackable)
.env
.env.*
!.env.example
!.env*.example
