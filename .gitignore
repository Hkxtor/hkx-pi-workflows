node_modules/
.DS_Store
*.log

# Local pi / agent state (may hold MCP tokens after apply-mcp-profile)
.pi/
.pi-subagents/

# Local scratch only — versioned suite lives in scripts/tests/ (npm test)
scripts/_smoke/

# Secrets and local env (keep examples trackable)
.env
.env.*
!.env.example
!.env*.example
